/** Absolute path to the server directory, injected by Vite at build time (dev only) */
declare const __DEV_SERVER_DIR__: string;
/** Client app version injected at build time */
declare const __CLIENT_VERSION__: string;
/** Client app build identifier injected at build time */
declare const __CLIENT_BUILD__: string;
